trigger AccountTrigger1 on Account (after insert,after update) {
   /* if(Trigger.isinsert && Trigger.isafter){
            AccountHandlerClass.createContacts(Trigger.new);
    }
    AccountHandlerClass.sendEmail(Trigger.new); */
    }